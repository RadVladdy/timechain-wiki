{
  "name": "timechain-wiki",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "python3 sync-kb.py",
    "dev": "astro dev",
    "build": "npm run sync && astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "nostr-tools": "^2.23.12",
    "remark-wiki-link": "^2.0.1"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
